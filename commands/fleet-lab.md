---
description: /fleet-lab [frontier:wall,… | go | stop | status] — research-driven perpetual frontier lab — each lane is a blocking frontier; rounds gate cheap research (web/arxiv/census) before expensive implement/measure, record to SSOT, then re-research or wall. Walls are measured + reopenable. (Subcommand of /fleet — `/fleet lab …` is equivalent.) Triggers — "fleet lab", "플릿 랩", "연구 함대", "research 반복 루프", "프론티어 연구", "/fleet-lab".
argument-hint: "[frontier:wall,… | go | stop | status]"
allowed-tools: Bash
---

!`command -v sidecar >/dev/null 2>&1 && sidecar fleet lab $ARGUMENTS || echo "sidecar CLI not found — install dancinlab/sidecar (~/.sidecar/cli + ~/.local/bin/sidecar on PATH)"`
