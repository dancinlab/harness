---
description: /docs [status|check|scratch [name]] — single-doc discipline — architecture SSOT + log + scratch + quickref counts. Triggers — "문서 상태", "docs check", "단일문서 규율", "/docs".
argument-hint: "[status|check|scratch [name]]"
allowed-tools: Bash
---

!`command -v sidecar >/dev/null 2>&1 && sidecar docs $ARGUMENTS || echo "sidecar CLI not found — install dancinlab/sidecar (~/.sidecar/cli + ~/.local/bin/sidecar on PATH)"`
