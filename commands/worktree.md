---
description: /worktree {scan|gc|guard <cmd>} — worktree hygiene — flag stranded worktrees · auto-sweep merged (no-pileup/no-stranded). Triggers — "워크트리 정리", "worktree gc", "stranded worktree", "/worktree".
argument-hint: "{scan|gc|guard <cmd>}"
allowed-tools: Bash
---

!`command -v harness >/dev/null 2>&1 && harness worktree $ARGUMENTS || echo "harness CLI not found — install dancinlab/harness (~/.harness/cli + ~/.local/bin/harness on PATH)"`
