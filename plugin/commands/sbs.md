---
description: /sbs [auto[:<axis>]|manual] [<task>] — step-by-step plan-first runbook — decompose a task into a verified plan before acting (mode via recommend resolve-mode). Triggers — "단계별로", "step by step", "계획 먼저", "plan first", "sbs", "차근차근", "/sbs".
argument-hint: "[auto[:<axis>]|manual] [<task>]"
allowed-tools: Bash
---

!`command -v harness >/dev/null 2>&1 && harness sbs $ARGUMENTS || echo "harness CLI not found — install dancinlab/harness (~/.harness/cli + ~/.local/bin/harness on PATH)"`
