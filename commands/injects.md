---
description: /injects — per-turn inject footprint report (per-source bytes/tokens + aggregate vs injectBudgetBytes). Context-rot budget visibility. Triggers — "inject 총량", "context budget", "injects", "/injects".
allowed-tools: Bash
---

!`command -v sidecar >/dev/null 2>&1 && sidecar injects || echo "sidecar CLI not found — install dancinlab/sidecar (~/.sidecar/cli + ~/.local/bin/sidecar on PATH)"`
