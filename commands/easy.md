---
description: /easy {show|inject} — the friendly 'easy' response style (icon·alias·analogy·ASCII·compare). Triggers — "이지 모드", "쉽게 설명", "easy style", "친근하게", "/easy".
argument-hint: "{show|inject}"
allowed-tools: Bash
---

!`command -v harness >/dev/null 2>&1 && harness easy $ARGUMENTS || echo "harness CLI not found — install dancinlab/harness (~/.harness/cli + ~/.local/bin/harness on PATH)"`
