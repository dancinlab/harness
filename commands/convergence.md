---
description: /convergence {status|recompute|by-category} — optional incident-convergence tracker (recurring-defect learning). Triggers — "수렴 추적", "convergence", "재발 추적", "/convergence".
argument-hint: "{status|recompute|by-category}"
allowed-tools: Bash
---

!`command -v sidecar >/dev/null 2>&1 && sidecar convergence $ARGUMENTS || echo "sidecar CLI not found — install dancinlab/sidecar (~/.sidecar/cli + ~/.local/bin/sidecar on PATH)"`
