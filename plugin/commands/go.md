---
description: /go — continue the most-recently proposed action without re-confirming. Triggers — "고", "계속", "진행해", "go ahead", "continue", "/go".
allowed-tools: Bash
---

!`command -v harness >/dev/null 2>&1 && harness go $ARGUMENTS || echo "harness CLI not found — install dancinlab/harness (~/.harness/cli + ~/.local/bin/harness on PATH)"`
