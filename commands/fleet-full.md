---
description: /fleet-full [frontier:goal,… | parallel | go | stop | status] — full-stack perpetual campaign weaving ALL THREE phases (🔬research → 🛠️implement → 🧅abstraction) per frontier, in order — implement is NEVER skipped (research→abstract direct jump is forbidden lazy-ceiling; a weak lever still runs the cheapest local probe to MEASURE the wall before abstraction). Runs SEQUENTIAL by default (one frontier/phase at a time, foreground/afg-style); pass `parallel` to fan out via Workflow. Cheap/local implement auto-proceeds; only paid (pool/GPU-rent/large sweep) implement gates on an explicit go. Walls (🧱) and meta-laws (🌌) both reopenable; depletion needs BOTH axes dry (c14). (Subcommand of /fleet — `/fleet full …` is equivalent.) Triggers — "fleet full", "풀스택 함대", "전체 캠페인", "리서치+구현+추상 순환", "/fleet-full".
argument-hint: "[frontier:goal,… | parallel | go | stop | status]"
allowed-tools: Bash
---

!`command -v sidecar >/dev/null 2>&1 && sidecar fleet full $ARGUMENTS || echo "sidecar CLI not found — install dancinlab/sidecar (~/.sidecar/cli + ~/.local/bin/sidecar on PATH)"`
