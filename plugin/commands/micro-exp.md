---
description: /micro-exp [<scope>] — context-driven micro-experiment sweep — infra-gate → budget → dispatch → monitor → absorb → ledger. Triggers — "마이크로 실험", "micro experiment", "작은 실험 스윕", "/micro-exp".
argument-hint: "[<scope>]"
allowed-tools: Bash
---

!`command -v harness >/dev/null 2>&1 && harness micro-exp $ARGUMENTS || echo "harness CLI not found — install dancinlab/harness (~/.harness/cli + ~/.local/bin/harness on PATH)"`
