---
description: /gc [scan|drift] — broken-markdown-link detection across guide docs. Triggers — "링크 검사", "broken links", "gc scan", "/gc".
argument-hint: "[scan|drift]"
allowed-tools: Bash
---

!`command -v sidecar >/dev/null 2>&1 && sidecar gc $ARGUMENTS || echo "sidecar CLI not found — install dancinlab/sidecar (~/.sidecar/cli + ~/.local/bin/sidecar on PATH)"`
