---
description: /abg [labels] — all-bg-go — fan out the prior turn’s proposed branches as PARALLEL background Agents. Triggers — "전부 백그라운드", "병렬 실행", "all background", "fan out parallel", "abg", "/abg", "/all-bg-go".
argument-hint: "[labels]"
allowed-tools: Bash
---

!`command -v harness >/dev/null 2>&1 && harness abg $ARGUMENTS || echo "harness CLI not found — install dancinlab/harness (~/.harness/cli + ~/.local/bin/harness on PATH)"`
