---
description: /easy {show|inject} — the friendly 'easy' response style (icon·alias·analogy·ASCII·compare). Triggers — "이지 모드", "쉽게 설명", "easy style", "친근하게", "/easy".
argument-hint: "{show|inject}"
allowed-tools: Bash
---

!`command -v sidecar >/dev/null 2>&1 && sidecar easy $ARGUMENTS || echo "sidecar CLI not found — install dancinlab/sidecar (~/.sidecar/cli + ~/.local/bin/sidecar on PATH)"`
