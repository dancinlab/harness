---
description: /prefs {show|code <lang>|docs <lang>|response <lang>|inject} — language preferences across 3 axes (code · docs · response). Triggers — "언어 설정", "prefs", "응답 언어", "한국어로", "/prefs".
argument-hint: "{show|code <lang>|docs <lang>|response <lang>|inject}"
allowed-tools: Bash
---

!`command -v harness >/dev/null 2>&1 && harness prefs $ARGUMENTS || echo "harness CLI not found — install dancinlab/harness (~/.harness/cli + ~/.local/bin/harness on PATH)"`
