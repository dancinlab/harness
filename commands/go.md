---
description: /go — continue the most-recently proposed action without re-confirming. Triggers — "고", "계속", "진행해", "go ahead", "continue", "/go".
allowed-tools: Bash
---

!`command -v sidecar >/dev/null 2>&1 && sidecar go $ARGUMENTS || echo "sidecar CLI not found — install dancinlab/sidecar (~/.sidecar/cli + ~/.local/bin/sidecar on PATH)"`
