---
description: /gap [full|list|<scope>] — multi-axis gap exploration — 40 breakthrough lenses (8 families), triage → deepen runbook. Triggers — "갭 분석", "돌파구 찾아", "gap analysis", "breakthrough lenses", "막힌데 뚫어", "/gap", "/kick".
argument-hint: "[full|list|<scope>]"
allowed-tools: Bash
---

!`command -v harness >/dev/null 2>&1 && harness gap $ARGUMENTS || echo "harness CLI not found — install dancinlab/harness (~/.harness/cli + ~/.local/bin/harness on PATH)"`
