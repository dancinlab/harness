---
description: /end — session-closure safety check — uncommitted · unpushed · stash · open PRs · branches · worktrees. Triggers — "세션 마무리", "끝내기 전 점검", "end session", "closure check", "/end".
allowed-tools: Bash
---

!`command -v harness >/dev/null 2>&1 && harness end $ARGUMENTS || echo "harness CLI not found — install dancinlab/harness (~/.harness/cli + ~/.local/bin/harness on PATH)"`
