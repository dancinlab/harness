---
description: /bitter-gate audit [window] — rule-hit frequency → retire dormant enforcement rules. Triggers — "규칙 빈도 감사", "bitter-gate", "dormant rule", "/bitter-gate".
argument-hint: "audit [window]"
allowed-tools: Bash
---

!`command -v harness >/dev/null 2>&1 && harness bitter-gate $ARGUMENTS || echo "harness CLI not found — install dancinlab/harness (~/.harness/cli + ~/.local/bin/harness on PATH)"`
