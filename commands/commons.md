---
description: /commons {inject|show} — always-on cross-project governance SSOT (config/commons.md; repo override .harness/commons.md). Triggers — "공용 거버넌스", "commons show", "/commons".
argument-hint: "{inject|show}"
allowed-tools: Bash
---

!`command -v sidecar >/dev/null 2>&1 && sidecar commons $ARGUMENTS || echo "sidecar CLI not found — install dancinlab/sidecar (~/.sidecar/cli + ~/.local/bin/sidecar on PATH)"`
