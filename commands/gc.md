---
description: /gc [scan|drift] — broken-markdown-link detection across guide docs. Triggers — "링크 검사", "broken links", "gc scan", "/gc".
argument-hint: "[scan|drift]"
allowed-tools: Bash
---

!`command -v harness >/dev/null 2>&1 && harness gc $ARGUMENTS || echo "harness CLI not found — install dancinlab/harness (~/.harness/cli + ~/.local/bin/harness on PATH)"`
