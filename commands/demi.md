---
description: /demi — design-architecture program runbook — the 7-verb spine (spec→structure→design→analyze→synthesize→verify→handoff). Triggers — "설계 파이프라인", "demi", "7-verb", "아키텍처 설계 런북", "/demi".
allowed-tools: Bash
---

!`command -v harness >/dev/null 2>&1 && harness demi $ARGUMENTS || echo "harness CLI not found — install dancinlab/harness (~/.harness/cli + ~/.local/bin/harness on PATH)"`
