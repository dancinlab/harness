---
description: /audit [full|summary|json] — 6-axis sidecar self-scorecard (/60). Triggers — "감사", "스코어카드", "audit", "self-score", "/audit".
argument-hint: "[full|summary|json]"
allowed-tools: Bash
---

!`command -v sidecar >/dev/null 2>&1 && sidecar audit $ARGUMENTS || echo "sidecar CLI not found — install dancinlab/sidecar (~/.sidecar/cli + ~/.local/bin/sidecar on PATH)"`
